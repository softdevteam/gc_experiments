KrunEntry = (
    run_iter: args = (
        | bodies innerIterations |
        Body initialize.
        bodies := NBodySystem new.
        innerIterations := (args at: 2) asInteger.
        innerIterations timesRepeat: [ bodies advance: 0.01 ].
    )
)
